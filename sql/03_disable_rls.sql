-- TEMPORARY: Disable RLS to verify if policies are the blocker
-- WARNING: This makes the table public. We will re-enable it after debugging.
ALTER TABLE public.weekly_patterns DISABLE ROW LEVEL SECURITY;
